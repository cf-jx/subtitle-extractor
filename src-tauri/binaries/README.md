# Runtime sidecars

This directory is populated by the pinned build scripts:

- `ffmpeg-$TARGET_TRIPLE`
- `ffprobe-$TARGET_TRIPLE`
- `yt-dlp-$TARGET_TRIPLE`

Windows sidecars use the `.exe` suffix. Generated binaries are intentionally
excluded from Git.
